PacificHome feed original is in XML format. Need to convert that to 
JSONL as required by dataconnect

Aug15:
 - Changed "availability" value to true/false (instead of "out of stock"/"in stock"/...)

Nov 14:
 - Added a check "if key == promotion", use 'promotionNow'.
 - Trying to debug why fq=promotion:"FREE SHIPPING!" returns zero results

Jun 21/2023
 - Used .xml generated using Python XML convertor
 - Contains only 15% products "onSale=true" (previously onSale was false but products had sale_price<price)

July 21/2023
 - Added 'lowstock' = True for ~15% products

Sept 05/2023
 - Remove 'by ...' (eg, 'by some_artist') to avoid copyright issues if any
 - Replace 'world market' with 'pacific' even if there is a blank space between world and market

Sept 15/2023
 - Added few more names to proper-name list (eg, Tyson)

Sept 21/2023
 - Replaced color = Multi to one of the standard colors, at random
 - Delete pids (and their variants) for which we could not generate openAI images

NOTE:
 - If any 'new' attributes are to be inserted into BR catalog, update the 'generateAttributeValues.py' and
 - run that first. Then change brsmFeed.py to include those new attributes in the product catalog
 - Then run the transform.py to actually generate new catalog with those new attributes

 - Since the prices are used in Engagement segementation rules, make sure not to change product
 - price (and sale_price). Otherwise it will impact Discovery's learning that has occured till then

